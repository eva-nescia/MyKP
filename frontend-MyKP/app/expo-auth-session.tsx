// This component handles the OAuth callback from the Expo auth session
// It doesn't need to do anything - the handleGoogleLogin function in LoginScreen.tsx
// handles the redirect and navigation after receiving the callback URL

export default function ExpoAuthSessionCallbackScreen() {
  return null;
}
